# OfficeAppscripts Setup

## Overview
This folder contains Google Apps Script tools for Office-like automation within Google Sheets.

## Prerequisites
- Node.js installed.
- Clasp CLI installed (`npm install -g @google/clasp`).
- Logged into Clasp (`clasp login`).

## Syncing
- Push changes: `clasp push`
- Open project: `clasp open`
